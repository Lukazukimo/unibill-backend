# Generated by scripts/gen_openapi.ts from spec §E — do not edit by hand.
# Regenerate: deno run --allow-read --allow-write scripts/gen_openapi.ts
openapi: 3.1.0
info:
  title: Unibill backend API
  version: 0.1.0
  description: >-
    User-facing & admin Edge Functions (spec §E). Internal workers (sync/extraction/capacity,
    cron-invoked) are not part of the public API. Authored from §E — request validation is
    hand-written, not Zod.
  license:
    name: Apache-2.0
    url: 'https://www.apache.org/licenses/LICENSE-2.0'
servers:
  - url: 'https://<project-ref>.supabase.co/functions/v1'
    description: Supabase Edge Functions
security:
  - bearerAuth: []
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Supabase user JWT (or service_role for admin/internal calls).
  schemas:
    Error:
      type: object
      properties:
        error:
          type: string
        detail:
          type: string
      required:
        - error
paths:
  /config/resolve:
    get:
      summary: Resolve a config key via the app_settings cascade
      parameters:
        - name: key
          in: query
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Resolved value + the scope it came from
          content:
            application/json:
              schema:
                type: object
                properties:
                  value: {}
                  scope_resolved_from:
                    type: string
                    enum:
                      - user
                      - household
                      - global
                      - default
        '401':
          description: missing/invalid JWT
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: key does not exist
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /emails/connect:
    post:
      summary: Connect a Gmail (IMAP) mailbox
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email_address:
                  type: string
                  format: email
                  maxLength: 254
                app_password:
                  type: string
                  description: 16-char Gmail app password
                household_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
              required:
                - email_address
                - app_password
                - household_ids
      responses:
        '200':
          description: Connected
          content:
            application/json:
              schema:
                type: object
                properties:
                  connected_email_id:
                    type: string
                    format: uuid
                  household_bindings:
                    type: array
                    items:
                      type: object
                      properties:
                        household_id:
                          type: string
                          format: uuid
                        is_default:
                          type: boolean
        '401':
          description: IMAP authentication failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: email already connected by another user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: validation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  '/emails/{id}/rotate-password':
    patch:
      summary: Rotate the stored app password (owner only)
      parameters:
        - &ref_0
          name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                new_app_password:
                  type: string
              required:
                - new_app_password
      responses:
        '200':
          description: Rotated
          content:
            application/json:
              schema:
                type: object
                properties:
                  rotated_at:
                    type: string
                    format: date-time
  '/emails/{id}':
    delete:
      summary: Soft-delete a connected email (owner or sys admin)
      parameters:
        - *ref_0
      responses:
        '200':
          description: Soft-deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  soft_deleted:
                    type: boolean
  /invitations/redeem:
    post:
      summary: Redeem a household invitation code
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                code:
                  type: string
                  minLength: 8
                  maxLength: 8
              required:
                - code
      responses:
        '200':
          description: Joined household
          content:
            application/json:
              schema:
                type: object
                properties:
                  household_id:
                    type: string
                    format: uuid
                  role:
                    type: string
                    enum:
                      - member
        '403':
          description: invited_email does not match
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: invalid or expired code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /admin/promote-system-admin:
    post:
      summary: Grant/revoke system-admin (sys admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                target_user_id:
                  type: string
                  format: uuid
                grant:
                  type: boolean
                reason:
                  type: string
              required:
                - target_user_id
                - grant
                - reason
      responses:
        '200':
          description: Done
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  audit_id:
                    type: string
                    format: uuid
        '403':
          description: caller is not a system admin
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: cannot demote the last system admin
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  '/admin/invoices/{id}/reextract':
    post:
      summary: Re-queue an invoice for extraction (sys admin or household member)
      parameters:
        - *ref_0
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                force:
                  type: boolean
      responses:
        '200':
          description: Queued
          content:
            application/json:
              schema:
                type: object
                properties:
                  queued:
                    type: boolean
                  idempotency_key:
                    type: string
  /privacy/export-my-data:
    post:
      summary: 'Export the caller''s personal data (LGPD, 1/day)'
      responses:
        '200':
          description: Export ready
          content:
            application/json:
              schema:
                type: object
                properties:
                  download_url:
                    type: string
                  expires_at:
                    type: string
                    format: date-time
        '413':
          description: export exceeds 500MB
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: rate limited (1/day)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /privacy/my-account:
    delete:
      summary: Delete the caller's account (LGPD right to erasure)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                confirmation_email:
                  type: string
                  format: email
                reason:
                  type: string
              required:
                - confirmation_email
      responses:
        '200':
          description: Deletion completed
          content:
            application/json:
              schema:
                type: object
                properties:
                  deletion_initiated:
                    type: boolean
        '400':
          description: confirmation_email mismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: caller is the last admin of a household
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /telemetry/ingest:
    post:
      summary: 'Ingest client telemetry (consent-gated, redacted)'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                events:
                  type: array
                  maxItems: 50
                  items:
                    type: object
                    properties:
                      event_type:
                        type: string
                      severity:
                        type: string
                      payload:
                        type: object
                    required:
                      - event_type
                      - payload
              required:
                - events
      responses:
        '200':
          description: Ingested
          content:
            application/json:
              schema:
                type: object
                properties:
                  ingested:
                    type: integer
  /health:
    get:
      summary: Health probe (public; service_role bearer adds internal metrics)
      security: []
      responses:
        '200':
          description: Healthy or degraded
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum:
                      - ok
                      - degraded
                      - down
                  timestamp:
                    type: string
                    format: date-time
                  db_ok:
                    type: boolean
                  queue_depths:
                    type: object
                  ai_chain_state:
                    type: string
                  capacity_status:
                    type: string
                  last_sync_run_minutes_ago:
                    type: integer
                required:
                  - status
                  - timestamp
        '503':
          description: Down
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum:
                      - down
                  timestamp:
                    type: string
                    format: date-time
                required:
                  - status
                  - timestamp
